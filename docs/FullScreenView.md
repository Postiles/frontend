### Convention for full-screen-view

- Should close the current full-screen-view and render itself
  in the constructor.

- Should use document.body as its container.

- Should unlisten event handlers and detach dom elements in .close()
